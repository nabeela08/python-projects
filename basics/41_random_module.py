import random

random_integer = random.randint(1,20)
print(random_integer)

random_float = random.random() 
print(random_float) #it will print random no between 0 to 1

#if we want a random number between 0 to 5
random_float = random.random()*5
print(random_float)
